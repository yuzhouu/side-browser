Use case: precise-object-edit. Edit target: this SideBrowser app icon preview. Make ONLY the following color changes: recolor the entire top red-and-yellow capsule to one continuous uniform neutral medium gray #9AA0A6, eliminating its color split; recolor the entire large green main-page rectangle on the LEFT to the same neutral medium gray #9AA0A6. Preserve the narrow RIGHT sidebar's existing vivid highlighted blue exactly as it appears in the reference (approximately Chrome blue #4285F4). The blue sidebar must be the only chromatic accent. Preserve all geometry, proportions, shape locations, gutters, corner radii, white rounded-square tile, pale neutral gray opaque canvas, image dimensions and framing. Clean flat solid fills, crisp smooth edges, no texture or gradients. One icon only, no labels, no letters, no watermark, no added elements. Output an entirely OPAQUE image, including all background and shape pixels, with no alpha extraction, transparent holes or checkerboard. This is a color-selection preview only.
