# 上架准备验证记录

## 中英文商店资料（2026-09-17）

- 中文与英文各 5 张 1280×800 说明图、1 张 440×280 小宣传图、1 张 1400×560 横幅；共用 128×128 图标。中文首图保留本次主体缩小 15%、底部三列强调文字的新版排版，英文使用同一源稿与独立文案。
- 英文原图使用当前 `dist/` 与独立 en-US Chrome for Testing 149.0.7827.55 配置采集，真实原生侧窗 360×645、DPR 2。实际采集未登录 ChatGPT、英文 Wikipedia、欢迎页、最近列表和浅深色更多菜单；切换标签后 ChatGPT 的 `performance.timeOrigin` 保持不变。来源与 SHA-256 在 `assets/source/en/capture.json`。未执行 AI 对话或登录。
- 英文 Google 搜索遇到 CAPTCHA，未用于成品；英文资料查询图使用 Time management 主页面与 Pomodoro Technique 侧窗。中文原图与来源记录保持原样。
- 通过现有 Playwright 渲染器检查所有图片加载、英文说明无残留中文、文字不越出画板；逐张查看英文成品和首图缩小预览。PNG 尺寸、RGB 无 alpha、图片哈希、每语言 8 张图的独立 ZIP 以及对应商店文案均已校验。
- 独立 Chrome 验证商店预览与官网素材页在 1440px／390px 下无横向溢出；实际点击语言切换、打开英文 PNG、下载英文首图并逐字节对照源文件、下载含 15 张 PNG 的双语素材 ZIP。页面无控制台错误。临时证据在 `/tmp/sidebrowser-bilingual-qa/`，不随资料分发。
- 随后按语言整理到 `zh-CN/` 与 `en/`，每个文件夹含文案、说明及 8 张 PNG（均含相同图标），双语素材 ZIP 因而包含 16 张 PNG。迁移前后全部图片 SHA-256 一致；重新验证预览链接、官网中英文图片下载、打包清单及 390px 排版。`manifest.json` 作为自动生成产物，沿用原 `assets/` 下清单的格式检查排除规则。
- 类型检查与扩展构建通过；官网静态检查通过 7 页、168 个链接／资源和中英文图片路由；相关文件格式与 diff 空白检查通过。此次没有重跑完整扩展交互回归，没有发布网站或提交商店。

## 1.0.0 版本与文案同步（2026-09-16）

- 扩展清单、npm 元数据、README、官网中英文版本文案、隐私政策、商店预览及提交说明已统一为 1.0.0。
- 本轮构建通过 TypeScript strict、54 项 Node 测试和格式检查；官网静态检查通过 7 个页面、166 个本地链接／锚点／资源及两种下载 ZIP。
- 独立 Chrome for Testing 149.0.7827.55 检查中英文首页与隐私页在 1280px／320px 的版本显示和无横向溢出；两个语言首页实际下载的扩展 ZIP 均为 1.0.0，商店预览显示与链接一致，无页面或控制台错误。截图位于本机临时目录 `/tmp/sidebrowser-version-1.0.0-qa`。
- 此次同步复用既有商店图片，未重新采集网页或重跑扩展完整交互回归；原始素材与功能验证记录保留如下。官网产物已在本地重建，线上更新需发布本次修改。

## 素材采集与功能回归（历史记录）

日期：2026-09-16；当时版本：1.6.5。以下记录对应素材采集时的源码；1.0.0 沿用这些截图，采集时间与图片哈希保持原始记录。

- `npm test`：TypeScript strict、Vite 构建、54 项 Node 测试全部通过。
- 独立配置运行 `scripts/browser-smoke.mjs`：Chrome for Testing 149.0.7827.55、简体中文。真实原生侧边栏 360px、320px／480px 模拟布局、打开与关闭网页、切标签保活、后台恢复、导航、最近列表、跨窗口、设置与主题、浏览器重启全部通过。无运行错误或未预期警告；保留既有 iframe sandbox 提示。原始回归截图位于本机 `/tmp/sidebrowser-store-release-qa`，临时目录不属于分发产物。
- ChatGPT 未登录首页、维基百科“番茄工作法”、Google 同词搜索均在独立配置原生侧边栏中实际显示。ChatGPT 切换主标签后 `performance.timeOrigin` 不变。没有发送 AI 问题、使用个人账号或操作日常浏览数据。
- Google 在后续一次重新采集时加载超时，因此保留本次任务中更早成功采集的 Google 搜索、最近列表与外观菜单实拍，没有用空白页替换成功素材。每张最终原图的时间和哈希记录在 `assets/source/capture.json`。这不是网站永久兼容性的承诺。
- 5 张功能说明图按 1280×800 导出；宣传图 440×280、1400×560；商店图标 128×128。图片保持真实 UI，功能说明与标注由可编辑 HTML/CSS 排版生成。文字重叠在视觉检查后已修正。
- 宣传图和说明截图使用无透明通道的 RGB PNG；商店图标使用透明背景。导出与资料打包脚本核对 PNG 尺寸、SHA-256、截图数量、七种语言描述长度与安装 ZIP 根目录。设计源稿和原图保留在 `store/`，不会进入扩展运行包。

网站加载及截图验证只覆盖当时公开页面的展示和指定行为，不包含 AI 登录／付费／对话全过程；商店审核和账号资料尚未执行。隐私政策随后由 GitHub Pages 上线，中英文页面均通过未登录访问与 HTTP 200 检查，见 `../docs/website.md`。
